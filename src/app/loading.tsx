import Spinner from "@/components/ui/spinner";

export default function LoadingDatabasepage() {
  return (
    <main className="flex h-screen w-full items-center justify-center">
      <Spinner />
    </main>
  );
}
